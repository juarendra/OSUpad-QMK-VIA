# Hardware and feature settings are declared in keyboard.json.
# The stm32-dfu metadata is used only to link the application at 0x08000000;
# this STM32F103 target is programmed through ST-Link/SWD, not USB DFU.
