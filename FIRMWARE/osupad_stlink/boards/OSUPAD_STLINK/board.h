// The STM32duino board header contains the pin, oscillator, and USB pull-up
// definitions required by this F103-compatible hardware. Only board.c is
// replaced: it omits STM32duino's persistent bootloader flag.
#pragma once

#include "../../../../platforms/chibios/boards/STM32_F103_STM32DUINO/board/board.h"
